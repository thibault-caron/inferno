name: Release

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build-agent-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create .env file
        run: |
          cat > .env << EOF
          POSTGRES_PASSWORD=test_password
          POSTGRES_USER=test_user
          POSTGRES_DB=test_db
          DB_PORT=5432
          SERVER_PORT=8888
          TLS=false
          COMPOSE_PROFILES=agent
          EOF

      - name: Build agent transport library
        run: |
          docker compose --profile agent run transport
          if [ $? -ne 0 ]; then
            echo "Transport build failed!"
            exit 1
          fi

      - name: Build agent binary
        run: |
          docker compose --profile agent run agent-builder
          if [ $? -ne 0 ]; then
            echo "Agent build failed!"
            docker compose logs agent-builder
            exit 1
          fi

      - name: List all Docker volumes
        run: |
          docker volume ls

      - name: Check volume contents
        run: |
          docker run --rm -v inferno_agent-build:/app alpine:latest ls -lhR /app

      - name: Install linuxdeploy
        run: |
          wget https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
          chmod +x linuxdeploy-x86_64.AppImage

      - name: Extract agent binary
        run: |
          docker run --rm \
            -v inferno_agent-build:/app \
            -v ${{ github.workspace }}/release-artifacts:/out \
            alpine:latest \
            cp /app/bin/agent /out/agent-binary

      - name: Verify binary
        run: |
          ls -lh release-artifacts/
          file release-artifacts/agent-binary

      - name: Create AppImage
        run: |
          mkdir -p appdir/usr/bin
          cp release-artifacts/agent-binary appdir/usr/bin/inferno-agent

          # Bundle CA certificate
          mkdir -p appdir/usr/share/inferno
          cp certs/ca.crt appdir/usr/share/inferno/ca.crt

          # Create minimal SVG icon
          mkdir -p appdir/usr/share/icons/hicolor/scalable/apps
          cat > appdir/usr/share/icons/hicolor/scalable/apps/inferno-agent.svg << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <svg width="64" height="64" version="1.1" xmlns="http://www.w3.org/2000/svg">
            <circle cx="32" cy="32" r="30" fill="#0066cc"/>
          </svg>
          EOF

          # Create .desktop file with Icon reference
          mkdir -p appdir/usr/share/applications
          cat > appdir/usr/share/applications/inferno-agent.desktop << 'EOF'
          [Desktop Entry]
          Type=Application
          Name=Inferno Agent
          Exec=inferno-agent
          Icon=inferno-agent
          Categories=System;Utility;
          Terminal=false
          NoDisplay=true
          EOF

          echo "AppDir contents before linuxdeploy:"
          ls -lhR appdir/

          ./linuxdeploy-x86_64.AppImage \
            --appdir=appdir \
            --output=appimage

          RESULT=$?
          echo "linuxdeploy exit code: $RESULT"

          echo "Contents after linuxdeploy:"
          ls -lh *.AppImage 2>/dev/null || echo "No AppImage found"

          exit $RESULT

      - name: Rename and upload
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          echo "VERSION: $VERSION"
          ls -lh *.AppImage

          # Rename to match release naming convention
          mv Inferno_Agent-x86_64.AppImage inferno-agent-linux-${VERSION}.AppImage
          ls -lh inferno-agent-linux-*.AppImage

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: agent-linux-appimage
          path: inferno-agent-linux-*.AppImage

  build-agent-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install MinGW, CMake, and OpenSSL
        run: |
          choco install -y mingw cmake
          choco install -y openssl
        shell: powershell

      - name: Find OpenSSL installation
        run: |
          # Check where OpenSSL was installed
          if (Test-Path "C:\Program Files\OpenSSL-Win64") {
            echo "Found OpenSSL at C:\Program Files\OpenSSL-Win64"
            ls "C:\Program Files\OpenSSL-Win64"
          } elseif (Test-Path "C:\Program Files (x86)\OpenSSL-Win32") {
            echo "Found OpenSSL at C:\Program Files (x86)\OpenSSL-Win32"
            ls "C:\Program Files (x86)\OpenSSL-Win32"
          } else {
            echo "OpenSSL not found in standard locations"
            Get-ChildItem "C:\" -Filter "*OpenSSL*" -Recurse 2>$null | head -20
          }
        shell: powershell

      - name: Configure CMake
        run: |
          mkdir build
          cd build
          $openssl_path = "C:\Program Files\OpenSSL-Win64"
          if (-not (Test-Path $openssl_path)) {
            $openssl_path = "C:\Program Files (x86)\OpenSSL-Win32"
          }
          cmake .. `
            -G "MinGW Makefiles" `
            -DCMAKE_BUILD_TYPE=Release `
            -DOPENSSL_ROOT_DIR="$openssl_path"
        shell: powershell

      - name: Build agent
        run: |
          cd build
          cmake --build . --config Release --target agent -j 4
        shell: powershell

      - name: Bundle DLLs and ca.crt
        run: |
          mkdir -p release-artifacts/inferno-agent-windows
          copy build\bin\agent.exe release-artifacts\inferno-agent-windows\
          copy certs\ca.crt release-artifacts\inferno-agent-windows\
          copy "C:\Program Files\OpenSSL-Win64\bin\libssl-3-x64.dll" release-artifacts\inferno-agent-windows\ -ErrorAction SilentlyContinue
          copy "C:\Program Files\OpenSSL-Win64\bin\libcrypto-3-x64.dll" release-artifacts\inferno-agent-windows\ -ErrorAction SilentlyContinue
          echo "Run: set SERVER_HOST=localhost ^& agent.exe" > release-artifacts\inferno-agent-windows\README.txt
        shell: powershell

      - name: Create ZIP
        run: |
          $version = "${{ github.ref }}".Replace("refs/tags/", "")
          Compress-Archive -Path release-artifacts\inferno-agent-windows -DestinationPath inferno-agent-windows-${version}.zip
        shell: powershell

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: agent-windows-exe
          path: inferno-agent-windows-*.zip
